<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Previous Versions</h3>
  </div>
  <%#= form_for new_doc, url: upload_version_matter_document_path(:matter_id => matter.id, :id => doc.id), :html => {:multipart => true} do |f| %>
  <div class="modal-body">
  
    <div class="span6">
      <div class="control-group">
        <div class="controls">
        <!-- <div class="row-fluid"> -->
        <!-- <div class="span4"> -->
          <table id="table_bug_report" class="table table-striped table-bordered table-hover">
            <thead>
              <tr>
                <!-- <th class="center">
                  <label>
                    <input type="checkbox" />
                    <span class="lbl"></span>
                  </label>
                </th> -->
                <th>Document Name</th>
                <th>Version</th>
                <th class="hidden-480">Uploaded By</th>

                <th class="hidden-phone">
                  <i class="icon-time hidden-phone"></i>
                  Created_at
                </th>
                <th>Actions</th>
              </tr>
            </thead>

            <tbody>
              <% @document_versions.each do |document| %>           
              <tr>
                <!-- <td class="center">
                  <label>
                    <input type="checkbox" />
                    <span class="lbl"></span>
                  </label>
                </td> -->

                <td>
                  <a href="#"><%= document.doc_identifier %></a>
                </td>
                <td><%= document.version %></td>
                <td class="hidden-480"><%= document.user.roleable.full_name if document.user%></td>
                <td class="hidden-phone"><%= document.created_at.strftime("%d %b. %Y")%></td>
                <td><%= link_to "Download","#"%>|<%= link_to "Destroy", delete_version_matter_document_path(:matter_id => document.matter_id, :id => document.id), method: :delete, data: { confirm: 'Are you sure, you want to delete this versions?' }%></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        <!-- </div> --><!--/span-->
        </div>
      </div>
    </div>
  </div><!--/row-->

          
                   
        <!-- </div>
      </div>
    </div> -->
    
    <!-- </div> -->
    <div class="modal-footer">
      
      <%#= f.submit "Submit", :class=>"btn btn-primary" %>
    </div>
  <%# end %> 
  <style>
  .modal {
 overflow-y: auto;
}

.modal-open {
 overflow: auto;
}
  </style> 